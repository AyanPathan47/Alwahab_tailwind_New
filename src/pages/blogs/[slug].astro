---
import BlogLayout from "../../layouts/Right_Blog_Layout.astro";
import CenterBlogLayout from "../../layouts/Center_Blog_Layout.astro";
import LeftLayout from "../../layouts/Left_Blog_Layout.astro";

const { frontmatter } = Astro.props;

// Map layout to layout components
const layouts = {
    BlogLayout,
    LeftLayout
};

console.log("Frontmatter layout:", frontmatter.layout); // Debug log
console.log("Resolved Layout:", layouts[frontmatter.customLayout]);
const Layout = layouts[frontmatter.customLayout] || BlogLayout;
---

<Layout>
    
</Layout>
