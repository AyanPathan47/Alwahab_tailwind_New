---
import BlogLayout from "../layouts/BlogLayout.astro";
import LeftLayout from  "../layouts/left-layout.astro";
import md from "../pages/blogs/try.md"

const { frontmatter, content } = Astro.props;

// Map layout to layout components
const layouts = {
    BlogLayout,
    LeftLayout
};
// console.log("Frontmatter:", frontmatter.title);
console.log("Content:", content);
// console.log("Frontmatter layout:", frontmatter.creationdate); // Debug log
// console.log("Resolved Layout:", layouts[frontmatter.layout]);
// const Layout = layouts[frontmatter.layout] || BlogLayout;
---
